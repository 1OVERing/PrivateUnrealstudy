@ 헤더 : <array>
@ 라이브러리 : Standard Tamplate Library ( 스탠다드 템플릿 라이브러리 )
@ 클래스 이름 : std::array
@ 컨테이너 방식 : Sequence Container
@ 특징 :
	ㆍ고정 크기 배열을 캡슐화 한 클래스이다.
	ㆍ{} 를 통해 초기화 할 수 있습니다. 
	ㆍ크기가 0 인 std::array 의 경우 (즉 N 이 0 일 때), array.begin() == array.end() 이다.
	ㆍ
	ㆍ
	ㆍ
	ㆍ
	ㆍ
	ㆍ


@ 추가 설명
고정 크기 배열을 캡슐화 한 템플릿 클래스이기에 선언시 Type과 Size가 필요하다.
일반 배열에 기능만 추가한 클래스이다.



@ 사용해야 하는 경우
	ㆍ일반 배열이 필요한 경우
	ㆍ
	ㆍ
	ㆍ

@ 궁금증
